#include <iostream>
using namespace std; //use this so that you don't need to write std::
int main1() {//specify the return type as java
    cout << "Hello world" ;
    cout << "Hello world" << endl; //endl: end line, allow you to go to a new line
    cout << "Hello world" ;

    return 0;//if return 0, it is error free, don't really need to type it as always have exit code 0(see console)
}//similar to java use bracket
//you cannot run this actually?